<div class="row align-center mb-5">
  {{#each posts as |post|}}
  <div class="col-md-6">
    <h3>
      {{post.title}}
    </h3>
    <p>
      {{post.contents}}
    </p>
    <p>
      {{post.User.name}}
    </p>
    <p>
      {{post.createdAt}}
    </p>
  </div>
  {{/each}}
</div>
