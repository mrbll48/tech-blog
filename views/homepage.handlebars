<div class="row align-center mb-5">
  {{#each posts as |post|}}
    <a href="/dashboard/{{post.id}}">
      <div>
        <h3 class="posts">
          {{post.title}}
        </h3>
        <p class="posts">
          {{post.contents}}
        </p>
        <p class="posts">
          {{post.User.name}}
        </p>
        <p class="posts">
          {{post.createdAt}}
        </p>
      </div>
    </a>
  {{/each}}
</div>